# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftTorrent' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftTorrent
  pod 'Bencode', :git => 'https://github.com/danieltmbr/Bencode.git'
  pod 'Alamofire', '~> 4.5'
  pod 'CryptoSwift', :git => 'https://github.com/krzyzanowskim/CryptoSwift.git', :branch => 'swift4'

  target 'SwiftTorrentTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
